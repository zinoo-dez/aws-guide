---
import BaseLayout from '../../layouts/BaseLayout.astro';
import Badge from '../../components/ui/Badge.astro';
import { getCollection } from 'astro:content';

const modules = await getCollection('modules');
const sortedModules = [...modules].sort((a, b) => a.data.moduleNumber - b.data.moduleNumber);
const caseStudies = await getCollection('caseStudies');
---

<BaseLayout 
  title="All Modules | AWS Architecture Guild"
  description="Explore the complete 11-module AWS cloud architecture curriculum from Foundations to Advanced Enterprise Capstones."
>
  <div class="modules-index-page container">
    <header class="page-header">
      <span class="section-kicker">Curriculum Directory</span>
      <h1 class="page-title">All 11 Learning Modules</h1>
      <p class="page-subtitle">
        Each module covers an essential domain of the AWS cloud ecosystem through core concepts and hands-on production case studies.
      </p>
    </header>

    <div class="modules-grid-full">
      {sortedModules.map((mod, idx) => {
        const modCases = caseStudies.filter(c => c.data.moduleId === mod.id);
        const staggerClass = `stagger-${(idx % 6) + 1}`;

        return (
          <div class={`module-full-card glass-card reveal-on-scroll ${staggerClass}`}>
            <div class="card-left">
              <div class="module-badge-row">
                <span class="mod-pill">Module {mod.data.moduleNumber}</span>
                <Badge variant="difficulty" type={mod.data.difficulty} size="sm" />
                <span class="mod-time">{mod.data.estimatedHours} Hours</span>
              </div>

              <h2 class="mod-title">
                <a href={`/modules/${mod.id}`}>{mod.data.title}</a>
              </h2>
              <p class="mod-tagline">{mod.data.tagline}</p>
              <p class="mod-desc">{mod.data.description}</p>

              <div class="mod-services-wrap">
                {mod.data.keyServices.map(svc => (
                  <span class="svc-chip">{svc}</span>
                ))}
              </div>
            </div>

            <div class="card-right">
              <span class="cases-count-label">{modCases.length} Included Case Studies</span>
              <div class="case-links-list">
                {modCases.map(cs => (
                  <a href={`/case-studies/${cs.id}`} class="mini-case-item">
                    <span class="case-item-title">{cs.data.title}</span>
                    <span class="case-item-time">{cs.data.estimatedMinutes}m</span>
                  </a>
                ))}
              </div>

              <a href={`/modules/${mod.id}`} class="btn btn-primary btn-block">
                <span>Explore Module {mod.data.moduleNumber}</span>
                <svg xmlns="http://www.w3.org/2000/svg" width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><polyline points="9 18 15 12 9 6"></polyline></svg>
              </a>
            </div>
          </div>
        );
      })}
    </div>
  </div>
</BaseLayout>

<style>
  .modules-index-page {
    padding-top: var(--space-10);
    padding-bottom: var(--space-16);
  }

  .page-header {
    text-align: center;
    margin-bottom: var(--space-12);
  }

  .page-title {
    font-size: clamp(2rem, 4vw, 3rem);
    color: var(--text-display);
    margin-bottom: var(--space-2);
  }

  .page-subtitle {
    font-size: 1.1rem;
    color: var(--text-secondary);
    max-width: 680px;
    margin: 0 auto;
  }

  .modules-grid-full {
    display: flex;
    flex-direction: column;
    gap: var(--space-6);
  }

  .module-full-card {
    display: grid;
    grid-template-columns: 1.5fr 1fr;
    gap: var(--space-8);
    padding: var(--space-6) var(--space-8);
    background: var(--bg-surface-1);
    border: 1px solid var(--border-medium);
    border-radius: var(--radius-lg);
    transition: transform var(--transition-fast), border-color var(--transition-fast);
  }

  .module-full-card:hover {
    border-color: var(--aws-orange-border);
  }

  .card-left {
    display: flex;
    flex-direction: column;
  }

  .module-badge-row {
    display: flex;
    align-items: center;
    gap: var(--space-3);
    margin-bottom: var(--space-3);
  }

  .mod-pill {
    font-family: var(--font-display);
    font-size: 0.75rem;
    font-weight: 700;
    text-transform: uppercase;
    letter-spacing: 0.05em;
    color: var(--aws-orange);
    background: var(--aws-orange-bg);
    border: 1px solid var(--aws-orange-border);
    padding: 0.2rem 0.6rem;
    border-radius: var(--radius-full);
  }

  .mod-time {
    font-size: 0.75rem;
    color: var(--text-muted);
    font-family: var(--font-mono);
  }

  .mod-title {
    font-size: 1.45rem;
    margin-bottom: 0.25rem;
  }

  .mod-title a {
    color: var(--text-display);
  }

  .mod-title a:hover {
    color: var(--aws-orange);
  }

  .mod-tagline {
    font-size: 0.95rem;
    font-weight: 600;
    color: var(--text-primary);
    margin-bottom: var(--space-2);
  }

  .mod-desc {
    font-size: 0.875rem;
    color: var(--text-secondary);
    line-height: 1.6;
    margin-bottom: var(--space-4);
    flex: 1;
  }

  .mod-services-wrap {
    display: flex;
    gap: 0.35rem;
    flex-wrap: wrap;
  }

  .svc-chip {
    font-size: 0.725rem;
    font-family: var(--font-mono);
    color: var(--text-secondary);
    background: var(--bg-surface-3);
    padding: 0.15rem 0.5rem;
    border-radius: var(--radius-sm);
    border: 1px solid var(--border-subtle);
  }

  .card-right {
    display: flex;
    flex-direction: column;
    background: var(--bg-surface-2);
    padding: var(--space-4) var(--space-5);
    border-radius: var(--radius-md);
    border: 1px solid var(--border-subtle);
  }

  .cases-count-label {
    font-size: 0.75rem;
    font-weight: 700;
    text-transform: uppercase;
    letter-spacing: 0.05em;
    color: var(--text-primary);
    margin-bottom: var(--space-3);
  }

  .case-links-list {
    display: flex;
    flex-direction: column;
    gap: 0.35rem;
    flex: 1;
    margin-bottom: var(--space-4);
  }

  .mini-case-item {
    display: flex;
    align-items: center;
    justify-content: space-between;
    padding: 0.35rem 0.55rem;
    background: var(--bg-surface-3);
    border-radius: var(--radius-sm);
    text-decoration: none;
    font-size: 0.8rem;
    transition: all var(--transition-fast);
  }

  .mini-case-item:hover {
    background: var(--bg-surface-hover);
    color: var(--aws-orange);
  }

  .case-item-title {
    color: var(--text-primary);
    white-space: nowrap;
    overflow: hidden;
    text-overflow: ellipsis;
    margin-right: 0.5rem;
  }

  .case-item-time {
    color: var(--text-muted);
    font-family: var(--font-mono);
    font-size: 0.7rem;
    flex-shrink: 0;
  }

  .btn-block {
    width: 100%;
  }

  @media (max-width: 860px) {
    .module-full-card {
      grid-template-columns: 1fr;
    }
  }
</style>
